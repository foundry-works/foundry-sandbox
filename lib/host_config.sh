#!/bin/bash

# Host config functions
# Note: Claude plugin configuration is now pre-baked into the Docker image.
# Credentials are copied directly from host by copy_configs_to_container().
# This file is kept for potential future host-side configuration needs.
